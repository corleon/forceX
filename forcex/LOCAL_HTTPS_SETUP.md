# Как включить HTTPS локально

## Для Local by Flywheel (рекомендуется)

### Способ 1: Через интерфейс Local (самый простой)

1. **Откройте Local by Flywheel**
2. **Выберите ваш сайт** (forcex)
3. **Нажмите на кнопку с тремя точками** (⋮) справа от названия сайта
4. **Выберите "Settings"** (Настройки)
5. **Перейдите на вкладку "SSL"**
6. **Включите переключатель "Enable HTTPS"**
7. **Нажмите "Save"** (Сохранить)
8. **Перезапустите сайт:**
   - Нажмите "Stop" (Остановить)
   - Подождите несколько секунд
   - Нажмите "Start" (Запустить)

9. **Откройте сайт** - теперь он должен работать по HTTPS

### Способ 2: Через командную строку (если способ 1 не работает)

1. **Откройте терминал/командную строку**
2. **Перейдите в папку Local:**
   ```bash
   cd "C:\Users\olegl\AppData\Local\Programs\Local"
   ```
   (Путь может отличаться - проверьте, где установлен Local)

3. **Запустите команду для включения SSL:**
   ```bash
   local-ssl enable
   ```

4. **Или для конкретного сайта:**
   ```bash
   local-ssl enable --site=forcex
   ```

---

## Проверка, что HTTPS работает

### 1. Проверьте URL в браузере:
- ✅ Должен начинаться с `https://` вместо `http://`
- ✅ В адресной строке должен быть значок 🔒 (замок)

### 2. Проверьте в админке WordPress:
- **Settings → General**
- **WordPress Address (URL)** - должен начинаться с `https://`
- **Site Address (URL)** - должен начинаться с `https://`

### 3. Проверьте в WooCommerce:
- Откройте **WooCommerce → Status**
- Предупреждение о HTTPS должно исчезнуть

---

## Если HTTPS не работает

### Проблема: Браузер показывает предупреждение о небезопасном соединении

**Решение:**
1. Это нормально для локальных сертификатов
2. Нажмите **"Advanced"** (Дополнительно)
3. Нажмите **"Proceed to forcex.local (unsafe)"** или **"Продолжить"**
4. Браузер запомнит ваш выбор

### Проблема: Сайт не открывается по HTTPS

**Решение:**
1. **Очистите кеш браузера:**
   - Ctrl + Shift + Delete
   - Выберите "Cached images and files"
   - Нажмите "Clear data"

2. **Попробуйте другой браузер**

3. **Проверьте настройки Local:**
   - Убедитесь, что SSL включен
   - Перезапустите сайт

### Проблема: Смешанный контент (Mixed Content)

**Решение:**
Если видите ошибки в консоли о "Mixed Content":
1. Это означает, что некоторые ресурсы загружаются по HTTP
2. Проверьте консоль браузера (F12) на наличие ошибок
3. Обновите настройки WordPress:
   - **Settings → General**
   - Убедитесь, что оба URL используют HTTPS
   - Сохраните изменения

---

## Альтернативные методы (если Local не работает)

### Вариант 1: Использовать плагин "Really Simple SSL"

1. **Установите плагин:**
   - WordPress Admin → Plugins → Add New
   - Найдите "Really Simple SSL"
   - Установите и активируйте

2. **Плагин автоматически настроит HTTPS**

### Вариант 2: Ручная настройка в wp-config.php

**ВНИМАНИЕ:** Используйте только если другие методы не работают!

1. **Откройте файл `wp-config.php`**
2. **Добавьте перед строкой "That's all, stop editing!":**

```php
// Force HTTPS for local development
if (isset($_SERVER['HTTP_X_FORWARDED_PROTO']) && $_SERVER['HTTP_X_FORWARDED_PROTO'] === 'https') {
    $_SERVER['HTTPS'] = 'on';
}

// Or force HTTPS for local domain
if (strpos($_SERVER['HTTP_HOST'], 'local') !== false) {
    $_SERVER['HTTPS'] = 'on';
}
```

3. **Сохраните файл**

---

## После включения HTTPS

### 1. Обновите настройки WordPress:

1. **Зайдите в админку:** `https://forcex.local/wp-admin`
2. **Settings → General**
3. **Измените URLs:**
   - WordPress Address (URL): `https://forcex.local`
   - Site Address (URL): `https://forcex.local`
4. **Сохраните изменения**

### 2. Очистите кеш:

- **Очистите кеш браузера** (Ctrl + Shift + Delete)
- **Если используете плагин кеширования** - очистите его кеш

### 3. Проверьте работу:

- ✅ Откройте сайт по HTTPS
- ✅ Попробуйте добавить товар в корзину
- ✅ Попробуйте оформить заказ
- ✅ Проверьте, что ошибка "Session expired" исчезла

---

## Проверка работы сессий после включения HTTPS

### 1. Откройте DevTools (F12)

### 2. Перейдите на вкладку Application (Chrome) или Storage (Firefox)

### 3. Проверьте Cookies:

Должны быть видны cookies:
- `woocommerce_items_in_cart`
- `woocommerce_cart_hash`
- `wp_woocommerce_session_*`

### 4. Проверьте свойства cookies:

- ✅ **Secure** - должен быть отмечен (для HTTPS)
- ✅ **Domain** - должен быть `.forcex.local` или `forcex.local`
- ✅ **Path** - обычно `/`

---

## Часто задаваемые вопросы

### Q: Нужно ли включать HTTPS локально?

**A:** Не обязательно, но рекомендуется:
- ✅ Помогает тестировать в условиях, близких к продакшену
- ✅ Решает проблемы с сессиями и cookies
- ✅ Позволяет тестировать функции, требующие HTTPS

### Q: Безопасно ли использовать самоподписанный сертификат?

**A:** Да, для локальной разработки это безопасно:
- ✅ Используется только на вашем компьютере
- ✅ Не используется на продакшене
- ✅ Помогает тестировать HTTPS функциональность

### Q: Что делать, если после включения HTTPS сайт не работает?

**A:**
1. Проверьте, что сайт запущен в Local
2. Очистите кеш браузера
3. Попробуйте открыть сайт в режиме инкогнито
4. Проверьте настройки WordPress URLs

### Q: Нужно ли что-то менять в коде после включения HTTPS?

**A:** Обычно нет:
- WordPress автоматически определит HTTPS
- WooCommerce автоматически использует HTTPS для cookies
- Код уже защищен от ошибок сессий

---

## Итог

### Рекомендуемые шаги:

1. ✅ **Включите SSL в Local by Flywheel** (через Settings → SSL)
2. ✅ **Перезапустите сайт**
3. ✅ **Обновите URLs в WordPress** (Settings → General)
4. ✅ **Очистите кеш браузера**
5. ✅ **Протестируйте оформление заказа**

### Результат:

- ✅ Сайт работает по HTTPS
- ✅ Сессии работают правильно
- ✅ Ошибка "Session expired" должна исчезнуть
- ✅ Условия тестирования ближе к продакшену

---

## Дополнительная помощь

Если у вас возникли проблемы:

1. **Проверьте документацию Local by Flywheel:**
   - https://localwp.com/help-docs/

2. **Проверьте логи:**
   - WooCommerce → Status → Logs
   - Консоль браузера (F12)

3. **Попробуйте переустановить SSL:**
   - В Local: Settings → SSL → Disable → Enable
